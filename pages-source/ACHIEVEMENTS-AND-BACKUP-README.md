
## Live production worker (added 2026-08-15)

Saved exact live `_worker.js` from production (advanced mode, ~580 lines).
Also `_routes.json` for Pages advanced mode.

Public API: /api/register, /api/subscribe, /api/contact, /api/posts, /api/events
Admin API: login, logout, subscribers, messages, posts, events, activity, registrations
Requires D1 binding name: DB
